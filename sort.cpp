#include <iostream>

void bubble_sort(int*);
void selection_sort(int*);
void insertion_sort(int*);
void merge_sort(int*);
void shell_sort(int*);
void quick_sort(int*);

int main()
{
    int* array = {4, 8, 2, 3, 6, 10, 1};
    return 0;
}
